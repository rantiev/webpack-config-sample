let userName = 'Fkn Idiot';
let btnAlert = `string text ${userName} string text`;
alert(btnAlert);
alert('${userName}');

let x = (a, b) => a + b;
alert(x(1, 2));

module.exports = function (x) {
    alert(x + btnAlert);
    alert(x + btnAlert);
};